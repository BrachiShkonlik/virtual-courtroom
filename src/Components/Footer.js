
export function Footer() {
    return (
        <>
            <div className="78888">
                <button className="cammera" onClick={() => }>מצלמה</button>
                <button className="microfon" onClick={() => }>מיקרופון</button>
                <button className="share" onClick={() => }>שיתוף</button>
            </div>
            <div className="">
                <button className="live" onClick={() => }>Live</button>
                <button className="timer" onClick={() => }><Timer></Timer></button>

            </div>

        </>
    )

}